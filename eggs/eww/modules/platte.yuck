(defvar theme "catppuccin") ;; {< replace_quoted(theme.color_name) >}
(defvar themes { ["bamboo", "dracula", "catppuccin", "onedarkpro", "tokyonight", "everforest", "solarized-osaka", "vscode", "wallpaper"] })

(defwindow platte
  :monitor 0
  :geometry (geometry :x 0
                      :y 0
                      :anchor "bottom center")
  :stacking "overlay"
  :exclusive false
  :focusable false
  :namespace "bar-center-popin"
  (box :orientation "v"
       :space-evenly false
       :class "centerpopinpanel"
       :height barh
       :spacing 6
       :valign "center"
       :halign "center"
    (for entry in { themes }
      (eventbox :cursor "pointer"
                :class { theme == entry ? "centericonactive" :  "centericon" }
                :height 36
                :width 172
                :onclick 'eww-changetheme sync ${entry}; eww-changetheme update'
        (label :text entry :class "plattetext")))))
